#parse("vm_include/init_var_entity.vm")
<?php
require_once 'commons/AbstractHref.php';

/**
 * HREF class for "$entity.name"
 * 
 * Created on $today.date ( Time $today.time )
 * Generated by $generator.name ( version $generator.version )
 *
 */
class ${entityHrefClass} extends AbstractHref {
    
    /**
     * Constructor
     */
    public function __construct() {
        parent::__construct("${entityClass}", "/${fn.toLowerCase(${entity.name})}.php");
    }
    
    /**
     * Builds the query string with ID parts
     * @param object ${entityVarName}
     * @return string
     */
    protected function hrefKeyParam(${entityVarName}) {
        ${DOLLAR}result = "";
#foreach( $attribute in $entity.keyAttributes )
        ${DOLLAR}result .= "&" . "${attribute.name}" . "=" . ${entityVarName}->${attribute.getterWithGetPrefix}() ;
#end
        return ${DOLLAR}result;
    }
    
}

?>